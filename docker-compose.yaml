services:

  user_service_mongodb:
    image: mongo:5.0
    volumes:
      - user_service_mongodb_container:/data/db
    logging:
      driver: none

  user_service:
    build: .
    depends_on:
      - user_service_mongodb
    ports:
      - "5000:80"
    volumes:
      - .:/code
    links:
      - user_service_mongodb

volumes:
  user_service_mongodb_container:
